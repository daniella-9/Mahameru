import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'channel_realization',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
